name: 'React CI pipeline'

on:
    push:
        branches:
          - main

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: 'Setting up node js'
              uses: actions/setup-node@v4
              with:
                node-version: '20'
